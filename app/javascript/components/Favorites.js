import React from "react"
import PropTypes from "prop-types"
class Favorites extends React.Component {
  render () {
    return (
      <React.Fragment>
        <div>This is the favorites component.</div>
      </React.Fragment>
    );
  }
}

export default Favorites
